<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MusMux.SelectionPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusMux"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SelectionPopup">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <StackPanel>
        <Grid Margin="16,8,10,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Choose Next Song"  Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0" Grid.Column="0"/>
            <Button HorizontalAlignment="Center" Grid.Row="0" Grid.Column="1" Click="Refresh_Click">
                <FontIcon Glyph="&#xE72C;"/>
            </Button>
            <Button HorizontalAlignment="Center" Grid.Row="0" Grid.Column="3" Click="Close_Click">
                <FontIcon Glyph="&#xE8BB;"/>
            </Button>
        </Grid>
        <ListView
            x:Name="SongListView"
            IsItemClickEnabled="True"
            ItemClick="SongListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SongItem">
                    <StackPanel Margin="0,5,0,5">
                        <TextBlock Text="{x:Bind Title}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind Artist}" Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
